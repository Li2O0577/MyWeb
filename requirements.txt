# Full project dependencies: Streamlit frontend + Flask backend.
# For manual full installation:
#   pip install -r requirements.txt
#
# For GPU training, reinstall PyTorch with the CUDA wheel that matches
# your machine: https://pytorch.org/get-started/locally/

-r requirements-frontend.txt
-r backend/requirements.txt
